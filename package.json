{
  "name": "ai-music-platform",
  "version": "1.0.0",
  "description": "AI Music Platform with Blockchain Integration",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "backend:install": "cd backend && npm install",
    "frontend:install": "cd frontend && npm install",
    "install:all": "npm install && npm run backend:install && npm run frontend:install",
    "test": "concurrently \"cd backend && npm test\" \"cd frontend && npm test\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 